import { PaymentMethodIconPipe } from './payment-method-icon.pipe';

describe('PaymentMethodIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentMethodIconPipe();
    expect(pipe).toBeTruthy();
  });
});
